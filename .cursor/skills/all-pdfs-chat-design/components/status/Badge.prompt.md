Small pill label for inline status/metadata ("Ready", "● 24 pp").

```jsx
<Badge tone="success">Ready</Badge>
<Badge tone="accent">Parsing…</Badge>
```

Tones: `neutral` (default), `accent`, `success`, `danger`. Keep labels short and sentence case.
