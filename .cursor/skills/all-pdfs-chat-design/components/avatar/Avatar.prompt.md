Round monogram avatar — the user menu in the top bar, or the brand "A" mark.

```jsx
<Avatar brand size={30} />        {/* brand logo glyph */}
<Avatar initials="JS" onClick={openMenu} />  {/* user */}
```

`brand` swaps to the accent-filled logo treatment. Neutral avatars use surface-2 + border.
