Labeled text input for auth and forms. Shows a pink focus ring on focus.

```jsx
<Input label="Email" type="email" placeholder="you@company.com" />
<Input label="Password" type="password" hint="At least 8 characters" />
```

Label is sentence case; `hint` renders dim text below. Uses `--surface` fill + hairline border at rest.
