# Grafana admin credentials — copy to /etc/all-pdfs-chat/monitoring.env on the VM (chmod 600).
# Referenced by the grafana service in docker-compose.monitoring.yml via env_file.
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=change-me-to-a-strong-password
