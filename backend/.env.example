# Application environment: dev | prod
# dev  -> LocalFileStorage + SQLite metadata (SqlPdfMetadataStore)
# prod -> Azure Blob + Azure SQL metadata (SqlPdfMetadataStore)
APP_ENV=dev

# SQLite metadata database (dev)
DATABASE_URL=sqlite+aiosqlite:///./data/app.db

# Azure SQL metadata database (prod) — paste from Azure portal (Connection strings)
AZURE_SQL_CONNECTIONSTRING=

# Max PDF upload size in bytes (default 10 MiB; max 100 MiB)
MAX_UPLOAD_SIZE_BYTES=10485760

# Azure Blob Storage (prod — required when APP_ENV=prod)
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER_NAME=pdfs

# Comma-separated origins for CORS (empty = disabled).
# Optional when the Next.js frontend uses rewrites (browser calls same-origin /api/v1).
CORS_ALLOWED_ORIGINS=

# Logging: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# Page classification + parsing (worker; PyMuPDF + pdfplumber)
CLASSIFICATION_ENABLED=true
CLASSIFICATION_MAX_PAGES=500

# Background worker (run: uv run python -m app.worker)
WORKER_POLL_INTERVAL_SECONDS=1
WORKER_LOCK_TTL_SECONDS=300
WORKER_MAX_ATTEMPTS=3

# Parsing: simple pages = local PyMuPDF; complex pages = Azure DI when enabled
PARSING_ENABLED=false
AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=
AZURE_DOCUMENT_INTELLIGENCE_API_KEY=
PARSING_POLL_INTERVAL_SECONDS=2
PARSING_MAX_WAIT_SECONDS=600
